/**
 *
 * {{properCase name }}Screen
 *
 */

import React from 'react';
import { View, Text } from 'react-native';
// import PropTypes from 'prop-types';

{{#if wantDucksAndSagas}}
import { connect } from 'react-redux';
import { Creators as {{ properCase name }}Creators } from 'store/ducks/{{ camelCase name }}';
{{/if}}
class {{ properCase name }}Screen extends {{{ type }}} {
  state = {
    text: 'Hello World',
  };

{{#if wantDucksAndSagas}}
  getInitialData = () => {
    const { dispatch } = this.props;
    dispatch({{ properCase name }}Creators.get{{ properCase name }}Request({ field1: 'hello', field2: 'world' }));
  };

  // componentDidMount(){
  //  this.getInitialData();
  // };
{{/if}}
  render() {
    const { text } = this.state;
    return (
      <View testID="container">
        <Text testID="container.text"{{#if wantDucksAndSagas}} onPress={this.getInitialData}{{/if}}>
          {text}
        </Text>
      </View>
    );
  }
}

// {{ properCase name }}Screen.propTypes = {
//   dispatch: PropTypes.func.isRequired,
// };

{{#if wantDucksAndSagas}}
const mapDispatchToProps = dispatch => ({ dispatch });

const mapStateToProps = ({ {{camelCase name}} }) => {
  const { data, loading, error } = {{camelCase name}};
  return { data, loading, error };
};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)({{ properCase name }}Screen);
{{else}}
export default {{ properCase name }}Screen;
{{/if}}
